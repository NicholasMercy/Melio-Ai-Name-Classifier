version: "3.9"
services:
  name_classifier:
    container_name: name_classifier
    image: local/name-classifier:latest
    command: --model_name=name-classifier
    working_dir: /app
    ports:
      - "8080:8080"
